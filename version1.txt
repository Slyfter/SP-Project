#squad = df.loc[1:, ['Squad','Comp']]
#comp = df.loc[1:, ['Comp']]
#print(df.head(5,))

#squad.drop_duplicates
#comp2 = comp.drop_duplicates(subset=['Comp'])

#group = df.groupby(by='Squad').RealGoals.sum()
#group2 = df.groupby(by='Squad').90s.sum()

#explanation why +1
#print(group+1)
#print(group2)
#print(squad)



#scatter diagram für goals + shots für each team
#Vergleich verschiedener ligen - welche liga ist am aggresivsten?